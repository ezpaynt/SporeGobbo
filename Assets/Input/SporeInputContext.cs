namespace SporeGobbo.Input
{
    public enum SporeInputContext
    {
        Gameplay,
        Wheel,
        Modal,
        Pause
    }

    public enum SporeControlScheme
    {
        Unknown,
        KeyboardMouse,
        Gamepad
    }
}
